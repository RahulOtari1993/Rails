<p id="notice"><%= notice %></p>
<h1>Sports</h1>
<table id='server-side-table' class="table table-striped">
</table>
<% if current_user && current_user.admin? %>
  <%= link_to 'New Sport', new_sport_path , class:"btn btn-dark" %>
<% end %>