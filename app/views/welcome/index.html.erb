<% if current_participant.present? %>
  <H1>Welcome, You are logged in successfully!</H1>

  <%= link_to('Logout Participant', destroy_participant_session_path, method: :delete) %>
<% else %>
  <H1>You need to login.</H1>
<% end %>
