<%= form_with(model: [@sport, sportannouncement], local: true) do |form| %>
  <% if sportannouncement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sportannouncement.errors.count, "error") %> prohibited this sportannouncement from beinx`` saved:</h2>

      <ul>
      <% sportannouncement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :msg %>
    <%= form.text_field :msg ,class: "form-control"%>
  </div>

<br>
  <div class="actions">
    <%= form.submit class:"btn btn-dark"%>
  </div>
<% end %>
