$('.participants-popup-container').html("<%= escape_javascript render 'admin/campaigns/participants/participant_modal' %>");
$('.filtered-participant-table').DataTable();
$('#filtered_participant').modal('show');
