$('#participant_note_description_input').val('');
$('#note-list-table').DataTable().ajax.reload();

<% if @message.present? %>
  Swal.fire({
    title: 'Add a Note',
    text: '<%= @message.to_s %>',
    confirmButtonClass: 'btn btn-primary',
    buttonsStyling: false,
  });
<% end %>
