
$('#facebook_media_posts_div').html("<%= j render '/participants/challenges/media_posts_content', challenge: @challenge, post_attachments: @posts_attachments %>")
$('#paginator').html('<%= escape_javascript(paginate(@posts_attachments, remote: true,  params: {controller: "challenges", action: "fetch_media_posts"}, first: 0, last: 0).to_s) %>');

$(document).ready(function () {
  // initialize gallery
  $('#social_feed_modal a.thumbnail').on('click', function () {
    $('.coin').removeClass('animated');
    $('#post_visit_checkmark').hide();
    updateGallery($(this));
  });
});
